# 확장 설계안 — 탐험·전투·데이트

> 2026-08-24. 「채집도 스토리처럼 / 포켓몬식 전투 / 데이트 모드」 요청에서 나온 설계.
> **아직 구현 안 함.** 순서를 정하려고 먼저 적는다.

## 지금 구조의 문제

```
탐험 = 지도에서 장소 클릭 → 빛나는 자원 클릭 → 골드·EXP 획득 → 끝
```
**이야기가 없다.** 클릭 노동이고, 히로인 조우(성벽 50%)만 예외적으로 이야기가 붙어 있다.
상황별 사건 8개를 넣어 숨통은 트였지만, **여전히 「채집」과 「이야기」가 따로 논다.**

## 목표 구조

```
탐험을 나간다
  ↓
① 이동 중 사건        상황 질문 · 히로인 조우 · 몬스터 조우
  ↓
② 몬스터를 만나면      턴제 전투 (포켓몬식)
  ↓
③ 목적지에서 채집      지금의 자원 클릭
  ↓
④ 돌아오는 길          또 사건이 걸릴 수 있음
```
**탐험 한 번이 짧은 이야기 하나가 된다.**

## 1. 턴제 전투 (포켓몬식)

지금 전투는 **방치형에 가깝다** — 자동 공격에 가드/스킬 타이밍만 누른다.
포켓몬식으로 가려면:

```
내 턴          [공격] [기술] [아이템] [도망]
  ↓
상대 턴         패턴에 따라 행동
  ↓
반복
```

**능력치가 여기서 처음으로 의미를 갖는다.**
```
힘 str    물리 피해
민첩 agi  선공 순서 · 회피
체력 vit  최대 HP
감각 per  치명타 · 상대 패턴 간파
지혜 wis  기술 위력 · 약점 간파
의지 will 상태이상 저항 · 도망 성공률
자비 karma 특정 선택지 개방 (전투 회피·설득)
```
→ 지금은 능력치가 **숫자로만 오르고 쓰이는 데가 없다.** 이게 제일 큰 낭비다.

**★자비(karma)로 「싸우지 않는 선택」을 열어주면** 마수가 「고갈의 증상」이라는 바이블 설정과 맞물린다.
목줄 찬 마수는 피해자니까, **죽이지 않고 목줄을 끊어주는 선택**이 있어야 한다.

## 2. 히로인 동행

지금은 성벽에서만 조우한다. 확장하면:

- **동행 시스템** — 호감도가 일정 이상이면 탐험에 데려갈 수 있다
- 동행 중에는 **전투에서 함께 싸우고**, 이동 중 **대화 이벤트**가 붙는다
- 히로인마다 **전수하는 기술이 다르다** (바이블: 엘리아나=검, 비비안=마법, 유안=궁, 세라=치유)
  → **인연이 곧 성장**이라는 바이블 설계가 여기서 구현된다

## 3. 질문 체계 3층

```
메인 스토리 선택지    EPS 안. 서사를 가른다
히로인 대화 (TALKS)   호감도. 히로인별로 따로. 지금 엘리아나 9개
상황별 사건 (SITU)    능력치·골드. 장소별. 지금 8개
```
→ **히로인이 늘면 TALKS 를 히로인별로 나눠야 한다.** 지금은 엘리아나 전용으로 굳어 있다.

## 4. 데이트 모드

```
호감도 45 (설렘)  →  데이트 개방
호감도 70 (연인)  →  데이트 종류 확대
호감도 92 (함락)  →  최종 이벤트
```

**데이트는 지금의 「일상 대화」와 다르다.**
- 장소를 고른다 (과수원 / 성벽 야경 / 장터 / 훈련장)
- 장소마다 전용 대화와 선택지
- 끝나면 **CG 1장** (바이블: 호감도 100마다 데이트 이벤트 + CG 1장)

지금 DAILY 6종이 있는데 **장소 선택이 없고 그냥 순서대로 열린다.** 그걸 장소 기반으로 바꾸는 작업.

---

# 제안 순서

**작은 것부터, 각 단계가 끝나면 플레이 가능해야 한다.**

| 순 | 작업 | 크기 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | **능력치를 전투에 연결** | 중 | 이미 쌓이고 있는데 안 쓰인다. 제일 아까운 것 |
| 2 | **턴제 전투로 교체** | 대 | 1번이 있어야 의미가 생긴다 |
| 3 | **탐험을 이야기 흐름으로** | 중 | 사건→전투→채집→귀환 |
| 4 | **자비로 비전투 해결** | 소 | 2번 위에 얹는다. 바이블 설정과 맞물림 |
| 5 | **데이트 모드** | 중 | 장소 선택 + CG |
| 6 | **히로인 동행** | 대 | 히로인이 둘 이상 있어야 의미가 산다 |

**1~2번이 이 게임의 체감을 제일 크게 바꾼다.** 능력치가 살아나고 전투가 선택의 문제가 된다.
6번은 2장 이후에 히로인이 늘고 나서 하는 게 맞다.
