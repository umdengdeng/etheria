# 검사 프로세스 — 네 관점

> 스토리를 쓰거나 질문지를 만들거나 게임 흐름을 손대면 **매번 이 넷을 돌린다.**
> 2026-08-24 도입(세 관점). 첫 실행에서 실제 버그 1건 + 설계 문제 8건이 나왔다.
> 2026-08-25 **④ 스토리보드 관점 추가.** 오프닝 개편 때 앞의 셋이 다 통과시킨 안에서
>   「암전이 네 줄 연속이라 그림이 안 움직인다」를 혼자 잡아냈다.
>
> ★**넷이 갈리면 그게 정보다.** 오프닝에서 미연시는 「히로인 얼굴 먼저」,
>   스토리는 「주인공 먼저」로 갈렸는데, 둘 다 앞에 두려니 길어진다는 게 드러났고
>   답은 「고르기」가 아니라 「압축」이었다.

## ① 스토리 작가 관점

- [ ] **인물이 이미 아는 것을 다른 인물이 또 알려주지 않는가** (정보 중복 → 긴장 하락)
- [ ] 설정이 앞뒤로 맞는가 — 특히 **기억·신체**처럼 세계관 근간에 닿는 것
- [ ] ★**고치는 방법이 문제 크기에 맞는가.** 대사 한 줄로 될 일에 장면을 넣지 말 것.
      장르 관습으로 암묵 처리하는 게 정답인 경우가 많다 (언어가 그랬다)
- [ ] 같은 감정 표현이 몇 번 반복되는가 (3회 넘으면 무뎌진다)
- [ ] 복선이 회수되는가, 회수 시점이 너무 이르지 않은가
- [ ] 대사가 그 인물의 성격·처지에서 나올 수 있는 말인가

## ② 미연시 전문가 관점

- [ ] **정답이 너무 뻔하지 않은가.** good/bad 가 한눈에 보이면 선택의 긴장이 없다
- [ ] 선택지가 전부 같은 모양(3지선다·good/ok/bad)으로 굳지 않았는가
- [ ] **수위가 튀는 질문이 단계 제한 없이 나오지 않는가** (첫 만남에 갑옷 끈?)
- [ ] 같은 질문이 반복 노출되는가 (본 것 재출현은 몰입을 깬다)
- [ ] 호감도가 **오르기만 하는가.** 내려갈 수 있어야 관계에 긴장이 생긴다
- [ ] 히로인의 성격(MBTI)이 정답 판정과 실제로 연결되는가

## ③ 게임 개발자 관점

- [ ] **보상 코드가 실제로 도는가** — 조건 분기에 걸려 안 도는 경로가 없는가
- [ ] 게이트(호감도·레벨)를 **의도치 않은 우회로 뚫을 수 있는가** (선물만 사서 스토리 스킵 등)
- [ ] 재화 획득량 대비 소모처 가격이 **반복 횟수로 몇 번인가** (지루함의 정체)
- [ ] 세이브/로드로 리롤 가능한 랜덤이 보상에 걸려 있는가
- [ ] 상한·하한에서 표시가 깨지지 않는가
- [ ] 새 필드를 추가했을 때 **기존 경로가 죽지 않는가** ← 이번 버그가 이것

## 실행 방법

1. `python tools/dump_story.py` 로 대사·선택지를 텍스트로 뽑는다 (없으면 수동)
2. 세 관점으로 각각 읽고 발견을 적는다
3. 코드로 확인 가능한 건 `node tools/check.js` 와 대조
4. 발견은 아래에 날짜별로 남긴다

---

## 2026-08-24 · 1회차 결과

**치명 1건 (수정 완료)**
- `vShowQ` 에서 `if(c.fx){...} else {호감도·골드}` 로 묶여 있어서, **fx 가 붙은 선택지는 호감도와 골드가 아예 안 올랐다.**
  대화 질문 4개(유혹 2·유머 1·성향 1)에 fx 를 붙인 순간부터 그 선택지들이 보상을 잃고 있었다.
  → `fx` 와 `fit` 을 분리해 둘 다 적용되게 고침.

**설계 문제 8건** — 아래 별도 정리 참고

### 조치 결과 (2026-08-24)

| # | 문제 | 조치 |
|---|---|---|
| 버그 | fx 붙은 선택지가 호감도·골드를 못 받음 | `fx`/`fit` 분리. 둘 다 적용 |
| ⑤ | 첫 만남에 수위 높은 질문이 뜸 | 질문마다 `minAff` 부여 (0/20/45/70) |
| ⑧ | 같은 질문 반복 노출 | `pickTalk()` — 안 본 것 우선, 한 바퀴 돌면 초기화 |
| ⑨ | 선물만 사서 스토리 스킵 | `GIFT_CAP=45` + 같은 선물 반복 시 25%씩 감쇠 |
| ① | 지구인이 아스란 말을 하는 게 미설명 | **대사 한 줄만 교체.** 처음엔 자각 장면 6줄을 넣었다가 되돌렸다 — 작은 걸 크게 만들어 흐름만 늘어졌다. 언어는 관습대로 암묵 처리하고, 문제를 만든 대사에서 언어 언급을 빼고 옷차림만 남겼다 |
| ② | 방금 지은 이름인데 "처음 듣는다" 아이러니 미사용 | 한 줄 추가 |
| ③ | 주인공이 아는 걸 엘리아나가 또 알려줌 | 그녀는 새 정보(빈도 증가)만 말하게 |

**남은 것** — ④반복표현 · ⑥뻔한정답 · ⑦호감도하락없음 · ⑩반복과다

## ④ 스토리보드 관점 (2026-08-25 추가)

**대사가 아니라 「그림이 어떻게 이어지는가」만 본다.**

- [ ] ★**같은 그림이 세 줄 넘게 이어지지 않는가.** 그림이 안 바뀌면 시간이 안 흐른다.
      암전·백색 같은 무배경일수록 더 빨리 지친다
- [ ] **컷 사이즈가 바뀌는가** (클로즈업 → 와이드 → 클로즈업).
      사이즈가 안 바뀌면 컷을 나눠도 한 컷처럼 보인다
- [ ] **카메라 높이가 컷마다 같지 않은가** (전부 아이레벨이면 단조롭다)
- [ ] **이어지는 두 컷의 색이 붙는가.** 흰 섬광 → 흰 공간처럼 같은 색이면
      끊지 말고 **디졸브로 살린다.** 반대로 검정 → 검정은 컷이 죽는다
- [ ] **대사가 약속한 것이 그림에 있는가** (→ `visual-claims.json` 과 연결)
- [ ] 한 컷에 정보가 하나인가. 두 개면 유저가 둘 다 놓친다
- [ ] 수치로 한 번 더: `python tools/vcheck.py`

**적용 예 — 오프닝 (2026-08-25)**
| 컷 | 그림 | 사이즈 | 왜 |
|---|---|---|---|
| 1 | 폰 든 손 | 클로즈업 | 유일한 광원. 첫 프레임이 즉시 읽힌다 |
| 2 | 암전 | — | 소리만. 시각을 끊어 청각을 세운다 |
| 3 | **방이 멀어짐** | 와이드 | ★암전 4줄 연속을 깨려고 넣은 컷. 「뜯겨나감」의 유일한 그림 |
| 4~5 | 암전 | — | 감각과 자조. 그림이 없어야 말이 들린다 |
| 6~7 | 백색 섬광 | — | 2번의 반대. 검정 다음 흰색이라 충격이 산다 |
| 8 | 세레니아 | 와이드 | 섬광이 그대로 공간이 된다 (디졸브) |
| 9 | 세레니아 | 클로즈업 | 「입술이 움직인다」 — 입술이니 붙어야 한다 |
